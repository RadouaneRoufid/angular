import { ImgCompressorDirective } from './img-compressor.directive';

describe('ImgCompressorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgCompressorDirective();
    expect(directive).toBeTruthy();
  });
});
